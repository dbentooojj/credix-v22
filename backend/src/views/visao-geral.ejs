<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Credix - Visao geral</title>
    <%- include('partials/favicon') %>
    <%- include('partials/toast') %>
    <%- include('partials/session-timeout') %>
    <style>html,body{background:#020617;} html{font-size:16px;}</style>
    <script>
        (function () {
            const root = document.documentElement;
            try {
                const savedTheme = localStorage.getItem('theme');
                if (savedTheme === 'dark') root.classList.add('dark');
                if (savedTheme === 'light') root.classList.remove('dark');
            } catch (error) {}
        })();
        window.tailwind = window.tailwind || {};
        window.tailwind.config = Object.assign({}, window.tailwind.config || {}, { darkMode: 'class' });
    </script>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body { font-family: 'Inter', sans-serif; overflow-x: hidden; color: #f8fbff; background:
            radial-gradient(72rem 44rem at -8% -12%, rgba(37,99,235,.26), transparent 48%),
            radial-gradient(48rem 32rem at 100% 0%, rgba(6,182,212,.12), transparent 44%),
            linear-gradient(180deg, #050b17 0%, #08111f 100%); }
        body::before { content:""; position:fixed; inset:0; pointer-events:none; opacity:.55; background:
            linear-gradient(180deg, rgba(255,255,255,.02), transparent 22%),
            radial-gradient(circle at 25% 20%, rgba(59,130,246,.08), transparent 24%); mix-blend-mode:screen; }
        .premium-header { background: rgba(12,18,32,.86); border-bottom: 1px solid rgba(148,163,184,.16); backdrop-filter: blur(10px); }
        .icon-btn { color:#cbd5e1; width:42px; height:42px; border-radius:12px; display:inline-flex; align-items:center; justify-content:center; transition:.12s; }
        .icon-btn:hover { color:#fff; background:rgba(59,130,246,.16); box-shadow: inset 0 0 0 1px rgba(96,165,250,.24); }
        .modern-sidebar { transform: translateX(0); transition: transform .12s ease-out; }
        .glass-card { position:relative; overflow:hidden; border:1px solid rgba(148,163,184,.14); border-radius:16px; background:linear-gradient(180deg, rgba(10,18,34,.96), rgba(9,16,30,.84)); box-shadow: 0 10px 30px rgba(2,6,23,.28); }
        .glass-card::before { content:""; position:absolute; inset:0; pointer-events:none; background:linear-gradient(180deg, rgba(255,255,255,.04), transparent 30%); opacity:.6; }
        .chip { display:inline-flex; align-items:center; gap:.4rem; padding:.38rem .68rem; border-radius:999px; border:1px solid rgba(96,165,250,.16); background:rgba(15,23,42,.62); color:#c7d2fe; font-size:.72rem; font-weight:700; }
        .page-title { font-size: clamp(2rem, 1.2vw + 1.2rem, 2.45rem); line-height: 1.04; font-weight: 700; letter-spacing: -.035em; }
        .page-subtitle { max-width: 54rem; color: #cbd5e1; font-size: .96rem; line-height: 1.6; }
        .overview-header-meta { display:grid; gap:.7rem; justify-items:start; }
        .header-period-chip { padding:.34rem .62rem; font-size:.69rem; font-weight:600; color:#cbd5e1; border-color:rgba(96,165,250,.12); background:rgba(15,23,42,.42); }
        .header-updated { color:#94a3b8; font-size:.82rem; line-height:1.4; }
        .header-updated-value { color:#cbd5e1; font-weight:600; }
        .metric-value { font-size: clamp(1.78rem, .7vw + 1.45rem, 2.1rem); line-height: 1.04; font-weight: 700; letter-spacing: -.04em; }
        .section-title { font-size: 1.06rem; line-height: 1.2; font-weight: 700; letter-spacing: -.02em; }
        .section-note { color: #cbd5e1; font-size: .88rem; line-height: 1.55; }
        .overview-top-card { position:relative; display:block; min-height:132px; border-radius:18px; padding:1.15rem 1.2rem; border:1px solid rgba(96,165,250,.2); overflow:hidden; box-shadow:0 16px 34px rgba(2,6,23,.24); }
        .overview-top-card::before { content:""; position:absolute; inset:0; pointer-events:none; background:linear-gradient(180deg, rgba(255,255,255,.05), transparent 42%); }
        .overview-top-card--cash { background:linear-gradient(135deg, rgba(14,41,105,.94), rgba(13,30,77,.9)); border-color:rgba(59,130,246,.38); }
        .overview-top-card--receivable { background:linear-gradient(135deg, rgba(4,93,74,.94), rgba(3,64,50,.9)); border-color:rgba(16,185,129,.34); }
        .overview-top-card--payable { background:linear-gradient(135deg, rgba(117,7,39,.94), rgba(85,7,32,.9)); border-color:rgba(244,63,94,.34); }
        .overview-top-card--projected { background:linear-gradient(135deg, rgba(78,17,133,.94), rgba(67,20,108,.9)); border-color:rgba(168,85,247,.32); }
        .overview-top-label { color:#e2e8f0; font-size:.8rem; font-weight:500; letter-spacing:.04em; text-transform:uppercase; opacity:.95; }
        .overview-top-value { margin-top:1rem; color:#f8fafc; font-size:2rem; line-height:1.04; font-weight:500; letter-spacing:-.03em; }
        .overview-top-note { margin-top:.5rem; color:#f8fafc; font-size:.98rem; line-height:1.35; opacity:.95; }
        .overview-top-meta { margin-top:.3rem; color:rgba(226,232,240,.8); font-size:.8rem; line-height:1.45; }
        .overview-top-icon { color:rgba(226,232,240,.85); font-size:1.05rem; }
        .flow-stat-card { border:1px solid rgba(71,85,105,.42); border-radius:16px; background:rgba(2,8,23,.24); padding:.95rem 1rem; }
        .flow-stat-label { color:#94a3b8; font-size:.72rem; font-weight:800; letter-spacing:.12em; text-transform:uppercase; }
        .flow-stat-value { margin-top:.55rem; color:#f8fafc; font-size:1.42rem; line-height:1.05; font-weight:700; letter-spacing:-.03em; }
        .flow-stat-note { margin-top:.4rem; color:#94a3b8; font-size:.82rem; line-height:1.45; }
        .daily-summary-panel { display:grid; gap:1rem; margin-top:1.15rem; }
        .daily-summary-card { position:relative; display:block; overflow:hidden; border-radius:18px; border:1px solid rgba(71,85,105,.4); padding:1.05rem 1.1rem; transition:border-color 160ms ease, transform 160ms ease, box-shadow 160ms ease; }
        .daily-summary-card:hover { transform:translateY(-1px); }
        .daily-summary-card::before { content:""; position:absolute; inset:0; pointer-events:none; background:linear-gradient(180deg, rgba(255,255,255,.05), transparent 42%); opacity:.7; }
        .daily-summary-card--incoming { background:linear-gradient(135deg, rgba(8,76,77,.9), rgba(12,54,64,.88)); border-color:rgba(16,185,129,.34); box-shadow:0 14px 28px rgba(6,78,59,.18); }
        .daily-summary-card--outgoing { background:linear-gradient(135deg, rgba(80,16,58,.9), rgba(60,18,53,.88)); border-color:rgba(244,63,94,.3); box-shadow:0 14px 28px rgba(76,5,25,.16); }
        .daily-summary-card--projected { background:linear-gradient(135deg, rgba(23,49,104,.9), rgba(26,50,93,.88)); border-color:rgba(96,165,250,.32); box-shadow:0 14px 28px rgba(30,64,175,.18); }
        .daily-summary-icon { display:flex; align-items:center; justify-content:center; width:2rem; height:2rem; border-radius:.8rem; font-size:.95rem; }
        .daily-summary-icon--incoming { background:rgba(16,185,129,.12); color:#6ee7b7; }
        .daily-summary-icon--outgoing { background:rgba(244,63,94,.12); color:#fda4af; }
        .daily-summary-icon--projected { background:rgba(96,165,250,.12); color:#93c5fd; }
        .daily-summary-label { color:#e2e8f0; font-size:.96rem; font-weight:500; line-height:1.35; }
        .daily-summary-value { margin-top:.9rem; color:#f8fafc; font-size:2rem; line-height:1.04; font-weight:500; letter-spacing:-.03em; }
        .daily-summary-value--incoming { color:#5eead4; }
        .daily-summary-value--outgoing { color:#fb7185; }
        .daily-summary-value--projected { color:#60a5fa; }
        .daily-summary-meta { margin-top:.55rem; color:rgba(226,232,240,.8); font-size:.84rem; line-height:1.45; }
        .chart-shell { position:relative; min-height:320px; border:1px solid rgba(71,85,105,.42); border-radius:18px; background:rgba(2,8,23,.26); padding:.9rem; }
        .chart-shell canvas { width:100% !important; height:290px !important; display:block; }
        .chart-empty { position:absolute; inset:.9rem; display:flex; align-items:center; justify-content:center; text-align:center; border-radius:14px; background:rgba(2,8,23,.72); color:#94a3b8; font-size:.9rem; line-height:1.6; }
        .recent-movements-table-shell { margin-top:1rem; overflow:hidden; border:1px solid rgba(71,85,105,.42); border-radius:18px; background:rgba(2,8,23,.22); }
        .recent-movements-table-wrap { overflow-x:auto; }
        .recent-movements-table-wrap::-webkit-scrollbar { height:10px; }
        .recent-movements-table-wrap::-webkit-scrollbar-thumb { background:rgba(71,85,105,.75); border-radius:999px; }
        .recent-movements-table { width:100%; min-width:980px; border-collapse:collapse; }
        .recent-movements-mobile-list { display:grid; gap:.75rem; margin-top:1rem; }
        .recent-movements-mobile-card { display:block; border:1px solid rgba(71,85,105,.42); border-radius:16px; background:rgba(2,8,23,.24); padding:.95rem 1rem; transition:border-color 160ms ease, transform 160ms ease, background-color 160ms ease; }
        .recent-movements-mobile-card:hover { transform:translateY(-1px); border-color:rgba(100,116,139,.55); background:rgba(15,23,42,.56); }
        .recent-movements-mobile-title { color:#f8fafc; font-size:.98rem; line-height:1.35; font-weight:700; }
        .recent-movements-mobile-subtitle { margin-top:.35rem; color:#94a3b8; font-size:.82rem; line-height:1.45; }
        .recent-movements-mobile-date { margin-top:.55rem; color:#94a3b8; font-size:.78rem; line-height:1.4; }
        .recent-movements-mobile-amount { font-size:1rem; line-height:1; font-weight:800; letter-spacing:-.02em; white-space:nowrap; }
        .recent-movements-head th { padding:1rem 1.15rem; text-align:left; color:#94a3b8; font-size:.76rem; font-weight:800; letter-spacing:.12em; text-transform:uppercase; border-bottom:1px solid rgba(51,65,85,.65); white-space:nowrap; }
        .recent-movements-head th:last-child, .recent-movements-head th:nth-last-child(2) { text-align:right; }
        .recent-movements-row { cursor:pointer; transition:background-color 160ms ease; }
        .recent-movements-row:hover { background:rgba(15,23,42,.54); }
        .recent-movements-row td { padding:1rem 1.15rem; border-top:1px solid rgba(30,41,59,.72); vertical-align:middle; }
        .recent-movements-date { color:#f8fafc; font-size:.98rem; font-weight:500; white-space:nowrap; }
        .movement-pill { display:inline-flex; align-items:center; min-height:26px; padding:0 .65rem; border-radius:999px; font-size:.68rem; font-weight:800; letter-spacing:.12em; text-transform:uppercase; }
        .movement-pill-neutral { border:1px solid rgba(100,116,139,.45); background:rgba(15,23,42,.58); color:#cbd5e1; }
        .movement-pill-in { border:1px solid rgba(16,185,129,.32); background:rgba(16,185,129,.12); color:#a7f3d0; }
        .movement-pill-out { border:1px solid rgba(248,113,113,.32); background:rgba(248,113,113,.12); color:#fecaca; }
        .recent-movements-description-title { color:#f8fafc; font-size:1rem; line-height:1.35; font-weight:500; }
        .recent-movements-description-subtitle { margin-top:.3rem; color:#94a3b8; font-size:.82rem; line-height:1.45; }
        .recent-movements-cell-right { text-align:right; white-space:nowrap; }
        .recent-movements-amount { font-size:1rem; line-height:1; font-weight:700; letter-spacing:-.02em; }
        .recent-movements-amount-in { color:#00f0b5; }
        .recent-movements-amount-out { color:#ff5d7b; }
        .recent-movements-amount-muted { color:#94a3b8; }
        .movement-pagination { display:flex; flex-direction:column; gap:.85rem; margin-top:1rem; }
        .movement-pagination-info { color:#94a3b8; font-size:.82rem; line-height:1.4; }
        .movement-pagination-controls { display:flex; flex-wrap:wrap; gap:.5rem; }
        .movement-pagination-btn { display:inline-flex; align-items:center; justify-content:center; min-width:2.35rem; min-height:2.35rem; padding:0 .78rem; border-radius:999px; border:1px solid rgba(71,85,105,.55); background:rgba(2,8,23,.3); color:#cbd5e1; font-size:.82rem; font-weight:700; transition:background-color 160ms ease, border-color 160ms ease, color 160ms ease, transform 160ms ease; }
        .movement-pagination-btn:hover { border-color:rgba(96,165,250,.4); background:rgba(30,41,59,.72); color:#f8fafc; transform:translateY(-1px); }
        .movement-pagination-btn[disabled] { opacity:.45; cursor:not-allowed; transform:none; }
        .movement-pagination-btn-active { border-color:rgba(124,58,237,.5); background:linear-gradient(135deg, rgba(109,40,217,.46), rgba(91,33,182,.4)); color:#fff; box-shadow:0 8px 20px rgba(76,29,149,.24); }
        .movement-pagination-ellipsis { display:inline-flex; align-items:center; justify-content:center; min-width:2rem; color:#64748b; font-size:.82rem; font-weight:700; }
        .recent-movements-loading .recent-movements-table-shell,
        .recent-movements-loading .recent-movements-mobile-list { opacity:.62; pointer-events:none; }
        .recent-movements-loading #recentMovementsPagination { opacity:.75; pointer-events:none; }
        .loading .js-value, .loading .js-note, .loading .js-chip { opacity: .35; }
        body.sidebar-collapsed .modern-sidebar { transform: translateX(-100%); }
        body.sidebar-collapsed main { margin-left: 0 !important; width: 100% !important; flex: 1 1 100% !important; }
        @media (max-width: 1024px) {
            .modern-sidebar { transform: translateX(-100%); }
            .modern-sidebar.mobile-open { transform: translateX(0) !important; }
            main { margin-left: 0 !important; width: 100% !important; flex: 1 1 100% !important; }
        }
        @media (min-width: 640px) {
            .movement-pagination { flex-direction:row; align-items:center; justify-content:space-between; }
            .movement-pagination-controls { justify-content:flex-end; }
        }
        @media (max-width: 767px) {
            .recent-movements-table-shell { display:none; }
        }
        @media (min-width: 768px) {
            .recent-movements-mobile-list { display:none; }
        }
        @media (min-width: 1024px) {
            .overview-header-meta { justify-items:end; text-align:right; }
        }
    </style>
    <link rel="stylesheet" href="/styles/premium-dashboard-theme.css?v=20260303f">
</head>
<body class="dashboard-premium typography-v1 dashboard-financial primary-gradient min-h-screen">
    <header class="premium-header fixed w-full top-0 z-50">
        <div class="flex items-center justify-between px-3 py-3 sm:px-6 sm:py-4">
            <div class="flex items-center">
                <button id="sidebarToggle" class="icon-btn focus:outline-none"><i class="fas fa-bars text-xl"></i></button>
                <h1 class="ml-3 sm:ml-6">
                    <a href="/admin/visao-geral.html" class="flex items-center" aria-label="Credix">
                        <%- include('partials/brand-wordmark', { size: 'header' }) %>
                    </a>
                </h1>
            </div>
            <div class="flex items-center gap-4">
                <button id="userMenuBtn" class="flex items-center gap-2 text-slate-200 hover:text-white focus:outline-none">
                    <span id="userName" class="font-semibold">Ola, <%= currentUser?.name || currentUser?.full_name || 'Administrador' %></span>
                    <i class="fas fa-chevron-down text-sm"></i>
                </button>
                <div id="userMenuDropdown" class="absolute right-6 top-full z-50 mt-2 hidden w-56 rounded-xl border border-slate-200 bg-white p-2 shadow-xl dark:border-slate-700 dark:bg-slate-900">
                    <button type="button" data-user-menu-action="profile" class="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-sm font-semibold text-slate-700 hover:bg-slate-100 dark:text-slate-200 dark:hover:bg-slate-800"><i class="fas fa-id-card text-slate-500 dark:text-slate-300"></i><span>Meu perfil</span></button>
                    <button type="button" data-user-menu-action="security" class="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-sm font-semibold text-slate-700 hover:bg-slate-100 dark:text-slate-200 dark:hover:bg-slate-800"><i class="fas fa-shield-halved text-slate-500 dark:text-slate-300"></i><span>Seguranca</span></button>
                    <button type="button" data-user-menu-action="help" class="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-sm font-semibold text-slate-700 hover:bg-slate-100 dark:text-slate-200 dark:hover:bg-slate-800"><i class="fas fa-circle-question text-slate-500 dark:text-slate-300"></i><span>Ajuda</span></button>
                    <div class="my-2 border-t border-slate-200 dark:border-slate-700"></div>
                    <button type="button" data-user-menu-action="logout" class="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-sm font-semibold text-rose-600 hover:bg-rose-50 dark:text-rose-400 dark:hover:bg-rose-950/40"><i class="fas fa-right-from-bracket"></i><span>Sair</span></button>
                </div>
            </div>
        </div>
    </header>

    <div class="flex pt-16 sm:pt-20">
        <%- include('partials/admin-sidebar', { activePage }) %>

        <main class="relative z-10 flex-1 p-4 sm:p-6 lg:p-8">
            <div id="overviewRoot" class="w-full">
                <section class="mb-7 flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between">
                    <div>
                        <h2 class="page-title">Visao geral</h2>
                        <p class="page-subtitle mt-2">Resumo do caixa, recebimentos e compromissos financeiros.</p>
                    </div>
                    <div class="overview-header-meta">
                        <span id="heroPeriodChip" class="chip header-period-chip js-chip">Periodo: --</span>
                        <div class="header-updated">Atualizado: <span id="heroGeneratedAt" class="header-updated-value js-value">--</span></div>
                    </div>
                </section>

                <div id="overviewError" class="mb-4 hidden rounded-2xl border border-rose-400/30 bg-rose-950/30 px-4 py-3 text-sm text-rose-200"></div>

                <section class="grid gap-4 md:grid-cols-2 xl:grid-cols-4">
                    <a id="overviewCashCard" href="/admin/dashboard.html" class="overview-top-card overview-top-card--cash">
                        <div class="relative z-10 flex items-start justify-between gap-3">
                            <p class="overview-top-label">Saldo em caixa</p>
                            <div class="flex items-center gap-2">
                                <span
                                    id="openCashAdjustModalBtn"
                                    role="button"
                                    tabindex="0"
                                    class="inline-flex min-h-[28px] items-center rounded-lg border border-slate-500/45 bg-slate-800/65 px-2 py-1 text-[11px] font-semibold text-slate-200 transition hover:bg-slate-700/35"
                                    aria-label="Ajustar caixa"
                                >
                                    <i class="fas fa-wallet mr-1"></i>Ajustar caixa
                                </span>
                                <span class="overview-top-icon"><i class="fas fa-wallet"></i></span>
                            </div>
                        </div>
                        <p id="overviewCashValue" class="overview-top-value js-value">--</p>
                        <p id="overviewCashNote" class="overview-top-note js-note">Disponivel agora</p>
                        <p id="overviewCashMeta" class="overview-top-meta js-note">--</p>
                    </a>

                    <a id="overviewReceivableCard" href="/admin/contas-a-receber.html" class="overview-top-card overview-top-card--receivable">
                        <div class="relative z-10 flex items-start justify-between gap-3">
                            <p class="overview-top-label">Contas a receber</p>
                            <span class="overview-top-icon"><i class="fas fa-arrow-up-right-from-square"></i></span>
                        </div>
                        <p id="overviewReceivableValue" class="overview-top-value js-value">--</p>
                        <p id="overviewReceivableNote" class="overview-top-note js-note">Emprestimos + Financeiro</p>
                        <p id="overviewReceivableMeta" class="overview-top-meta js-note">--</p>
                    </a>

                    <a id="overviewPayableCard" href="/admin/contas-a-pagar.html" class="overview-top-card overview-top-card--payable">
                        <div class="relative z-10 flex items-start justify-between gap-3">
                            <p class="overview-top-label">Contas a pagar</p>
                            <span class="overview-top-icon"><i class="fas fa-arrow-down-left"></i></span>
                        </div>
                        <p id="overviewPayableValue" class="overview-top-value js-value">--</p>
                        <p id="overviewPayableNote" class="overview-top-note js-note">Compromissos pendentes</p>
                        <p id="overviewPayableMeta" class="overview-top-meta js-note">--</p>
                    </a>

                    <a id="overviewProjectedCard" href="/admin/visao-geral.html" class="overview-top-card overview-top-card--projected">
                        <div class="relative z-10 flex items-start justify-between gap-3">
                            <p class="overview-top-label">Saldo previsto</p>
                            <span class="overview-top-icon"><i class="fas fa-arrow-trend-up"></i></span>
                        </div>
                        <p id="overviewProjectedValue" class="overview-top-value js-value">--</p>
                        <p id="overviewProjectedNote" class="overview-top-note js-note">Apos entradas e saidas</p>
                        <p id="overviewProjectedMeta" class="overview-top-meta js-note">--</p>
                    </a>
                </section>

                <section class="mt-4 grid gap-4 xl:grid-cols-[1.15fr_1.15fr_.8fr]">
                    <article class="glass-card p-4">
                    <div class="relative z-10 flex items-start justify-between gap-3">
                        <div>
                            <h3 class="section-title">Recebimentos de hoje</h3>
                            <p class="mt-2 text-sm leading-6 text-slate-300">Titulos previstos para entrada no dia atual.</p>
                        </div>
                        <span id="receiptsTodayTotal" class="chip js-chip">Total: --</span>
                    </div>
                    <div id="receiptsTodayList" class="relative z-10 mt-4 grid gap-3"></div>
                    </article>

                    <article class="glass-card p-4">
                    <div class="relative z-10 flex items-start justify-between gap-3">
                        <div>
                            <h3 class="section-title">Pagamentos de hoje</h3>
                            <p class="mt-2 text-sm leading-6 text-slate-300">Saidas financeiras previstas para hoje.</p>
                        </div>
                        <span id="paymentsTodayTotal" class="chip js-chip">Total: --</span>
                    </div>
                    <div id="paymentsTodayList" class="relative z-10 mt-4 grid gap-3"></div>
                    </article>

                    <article class="glass-card p-4">
                    <div class="relative z-10 grid gap-4">
                        <div>
                            <h3 class="section-title">Alertas financeiros</h3>
                            <p class="mt-2 text-sm leading-6 text-slate-300">Leitura rapida dos compromissos e riscos do dia.</p>
                        </div>

                        <div id="financialAlertsList" class="grid gap-3"></div>
                    </div>
                    </article>
                </section>

                <section class="mt-4 grid gap-4 xl:grid-cols-[1.45fr_.75fr]">
                    <article class="glass-card p-4">
                        <div class="relative z-10">
                            <div class="flex items-start justify-between gap-3">
                                <div>
                                    <h3 class="section-title">Fluxo mensal</h3>
                                    <p class="mt-2 text-sm leading-6 text-slate-300">Leitura dos ultimos meses com recebido, em aberto e atraso.</p>
                                </div>
                                <span class="chip">Ultimos 6 meses</span>
                            </div>

                            <div class="mt-4 grid gap-3 md:grid-cols-3">
                                <div class="flow-stat-card">
                                    <p class="flow-stat-label">Mes atual</p>
                                    <p id="flowCurrentValue" class="flow-stat-value js-value">--</p>
                                    <p id="flowCurrentNote" class="flow-stat-note js-note">Carregando fluxo atual.</p>
                                </div>
                                <div class="flow-stat-card">
                                    <p class="flow-stat-label">Comparativo</p>
                                    <p id="flowPreviousValue" class="flow-stat-value js-value">--</p>
                                    <p id="flowPreviousNote" class="flow-stat-note js-note">Carregando base anterior.</p>
                                </div>
                                <div class="flow-stat-card">
                                    <p class="flow-stat-label">Carteira do mes</p>
                                    <p id="flowExposureValue" class="flow-stat-value js-value">--</p>
                                    <p id="flowExposureNote" class="flow-stat-note js-note">Carregando exposicao do periodo.</p>
                                </div>
                            </div>

                            <div class="chart-shell mt-4">
                                <canvas id="overviewMonthlyChart" aria-label="Fluxo mensal" role="img"></canvas>
                                <div id="overviewChartEmpty" class="chart-empty hidden">
                                    <div>
                                        <i class="fas fa-chart-line mb-2 text-2xl opacity-60"></i>
                                        <p class="font-semibold">Sem dados no periodo.</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </article>

                    <article class="glass-card p-4">
                        <div class="relative z-10">
                            <div>
                                <h3 class="section-title">Resumo do dia</h3>
                                <p class="mt-2 text-sm leading-6 text-slate-300">Entradas, saidas e projecao imediata do caixa.</p>
                            </div>

                            <div class="daily-summary-panel">
                                <a id="summaryIncomingToday" href="/admin/contas-a-receber.html" class="daily-summary-card daily-summary-card--incoming">
                                    <div class="relative z-10 flex items-start gap-3">
                                        <span class="daily-summary-icon daily-summary-icon--incoming"><i class="fas fa-arrow-up-right-from-square"></i></span>
                                        <div class="min-w-0">
                                            <p class="daily-summary-label">Entradas previstas hoje</p>
                                            <p id="summaryIncomingTodayValue" class="daily-summary-value daily-summary-value--incoming js-value">--</p>
                                            <p id="summaryIncomingTodayMeta" class="daily-summary-meta js-note">Carregando entradas do dia.</p>
                                        </div>
                                    </div>
                                </a>

                                <a id="summaryOutgoingToday" href="/admin/contas-a-pagar.html" class="daily-summary-card daily-summary-card--outgoing">
                                    <div class="relative z-10 flex items-start gap-3">
                                        <span class="daily-summary-icon daily-summary-icon--outgoing"><i class="fas fa-arrow-down"></i></span>
                                        <div class="min-w-0">
                                            <p class="daily-summary-label">Saidas previstas hoje</p>
                                            <p id="summaryOutgoingTodayValue" class="daily-summary-value daily-summary-value--outgoing js-value">--</p>
                                            <p id="summaryOutgoingTodayMeta" class="daily-summary-meta js-note">Carregando saidas do dia.</p>
                                        </div>
                                    </div>
                                </a>

                                <a id="summaryProjectedToday" href="/admin/visao-geral.html" class="daily-summary-card daily-summary-card--projected">
                                    <div class="relative z-10 flex items-start gap-3">
                                        <span class="daily-summary-icon daily-summary-icon--projected"><i class="fas fa-wallet"></i></span>
                                        <div class="min-w-0">
                                            <p class="daily-summary-label">Saldo projetado do dia</p>
                                            <p id="summaryProjectedTodayValue" class="daily-summary-value daily-summary-value--projected js-value">--</p>
                                            <p id="summaryProjectedTodayMeta" class="daily-summary-meta js-note">Baseado no caixa atual e agenda do dia.</p>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        </div>
                    </article>
                </section>

                <section class="mt-4">
                    <article id="recentMovementsSection" class="glass-card p-4">
                        <div class="relative z-10">
                            <div class="flex items-start justify-between gap-3">
                                <div>
                                    <h3 class="section-title">Movimentacoes recentes</h3>
                                    <p class="mt-2 text-sm leading-6 text-slate-300">Apenas transacoes realizadas.</p>
                                </div>
                            </div>

                            <div class="recent-movements-table-shell">
                                <div class="recent-movements-table-wrap">
                                    <table class="recent-movements-table" aria-label="Movimentacoes recentes">
                                        <thead class="recent-movements-head">
                                            <tr>
                                                <th scope="col">Data</th>
                                                <th scope="col">Tipo</th>
                                                <th scope="col">Origem</th>
                                                <th scope="col">Descricao</th>
                                                <th scope="col">Entrada</th>
                                                <th scope="col">Saida</th>
                                            </tr>
                                        </thead>
                                        <tbody id="recentMovementsList"></tbody>
                                    </table>
                                </div>
                            </div>
                            <div id="recentMovementsMobileList" class="recent-movements-mobile-list"></div>
                            <div id="recentMovementsPaginationShell" class="movement-pagination hidden">
                                <p id="recentMovementsPageInfo" class="movement-pagination-info">Pagina --</p>
                                <div id="recentMovementsPagination" class="movement-pagination-controls"></div>
                            </div>
                        </div>
                    </article>
                </section>
            </div>
        </main>
    </div>

    <div id="cashAdjustModal" class="modal-base fixed inset-0 z-50 hidden items-center justify-center p-3 sm:p-4" data-modal-base="true" role="dialog" aria-modal="true" aria-labelledby="cashAdjustModalTitle">
        <div class="modal-base__panel modal-base__panel--md">
            <form id="cashAdjustForm" class="flex min-h-0 flex-1 flex-col">
                <div class="modal-base__header">
                    <div class="modal-base__title-wrap">
                        <h3 id="cashAdjustModalTitle" class="modal-base__title">Ajustar caixa</h3>
                        <p class="modal-base__description">Registre entrada ou retirada para atualizar o saldo atual.</p>
                    </div>
                    <button id="closeCashAdjustModalBtn" class="modal-base__close" type="button" aria-label="Fechar ajuste de caixa">
                        <i class="fas fa-times"></i>
                    </button>
                </div>

                <div class="modal-base__body">
                    <section class="modal-base__section">
                        <div class="modal-base__grid-2">
                            <div class="modal-base__field">
                                <label for="cashAdjustmentType" class="modal-base__field-label">Tipo</label>
                                <select id="cashAdjustmentType" class="filter-control" data-modal-autofocus>
                                    <option value="income">Entrada</option>
                                    <option value="expense">Saida</option>
                                </select>
                            </div>
                            <div class="modal-base__field">
                                <label for="cashAdjustmentAmount" class="modal-base__field-label">Valor (R$)</label>
                                <input id="cashAdjustmentAmount" type="number" min="0.01" step="0.01" inputmode="decimal" class="filter-control" placeholder="0,00" required>
                            </div>
                            <div class="modal-base__field">
                                <label for="cashAdjustmentDate" class="modal-base__field-label">Data</label>
                                <input id="cashAdjustmentDate" type="date" class="filter-control" required>
                            </div>
                            <div class="modal-base__field">
                                <label for="cashAdjustmentDescription" class="modal-base__field-label">Observacao</label>
                                <input id="cashAdjustmentDescription" type="text" maxlength="300" class="filter-control" placeholder="Motivo do ajuste (opcional)">
                            </div>
                        </div>
                    </section>
                </div>

                <div class="modal-base__footer">
                    <div class="modal-base__footer-secondary">
                        <button id="cancelCashAdjustModalBtn" type="button" class="modal-btn modal-btn--ghost">Cancelar</button>
                    </div>
                    <div class="modal-base__footer-actions">
                        <button id="saveCashAdjustmentBtn" type="submit" class="modal-btn modal-btn--primary">
                            <i class="fas fa-check mr-2"></i>Salvar ajuste
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <script>window.__AUTH_USER__ = <%- JSON.stringify(currentUser || null) %>;</script>
    <script>
        const currentUser = window.__AUTH_USER__;
        if (!currentUser) window.location.href = '/login';
        else {
            localStorage.setItem('isLoggedIn', 'true');
            localStorage.setItem('currentUser', JSON.stringify(currentUser));
            document.getElementById('userName').textContent = `Ola, ${currentUser.name || currentUser.full_name || 'Administrador'}`;
        }

        const sidebar = document.getElementById('sidebar');
        const main = document.querySelector('main');
        const sidebarToggle = document.getElementById('sidebarToggle');
        const userMenuBtn = document.getElementById('userMenuBtn');
        const userMenuDropdown = document.getElementById('userMenuDropdown');
        const overviewRoot = document.getElementById('overviewRoot');
        const overviewError = document.getElementById('overviewError');
        const recentMovementsSection = document.getElementById('recentMovementsSection');
        const recentMovementsPaginationShell = document.getElementById('recentMovementsPaginationShell');
        const recentMovementsPageInfo = document.getElementById('recentMovementsPageInfo');
        const recentMovementsPagination = document.getElementById('recentMovementsPagination');
        const recentMovementsMobileList = document.getElementById('recentMovementsMobileList');
        const openCashAdjustModalBtn = document.getElementById('openCashAdjustModalBtn');
        const cashAdjustModal = document.getElementById('cashAdjustModal');
        const cashAdjustForm = document.getElementById('cashAdjustForm');
        const closeCashAdjustModalBtn = document.getElementById('closeCashAdjustModalBtn');
        const cancelCashAdjustModalBtn = document.getElementById('cancelCashAdjustModalBtn');
        const saveCashAdjustmentBtn = document.getElementById('saveCashAdjustmentBtn');
        const cashAdjustmentTypeInput = document.getElementById('cashAdjustmentType');
        const cashAdjustmentAmountInput = document.getElementById('cashAdjustmentAmount');
        const cashAdjustmentDateInput = document.getElementById('cashAdjustmentDate');
        const cashAdjustmentDescriptionInput = document.getElementById('cashAdjustmentDescription');
        let overviewMonthlyChart = null;
        const overviewState = {
            recentMovementsPage: 1,
            recentMovementsPageSize: 6,
            recentMovementsTotalPages: 1,
            isPaginatingRecentMovements: false,
            cashAdjustmentSubmitting: false,
        };
        const isMobileViewport = () => window.matchMedia('(max-width: 1024px)').matches;

        function formatCurrency(value) {
            const safe = Number.isFinite(Number(value)) ? Number(value) : 0;
            return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL', minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(safe);
        }
        function formatPercent(value) {
            const safe = Number.isFinite(Number(value)) ? Number(value) : 0;
            return `${safe.toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}%`;
        }
        function formatDateTime(value, timeZone) {
            const date = new Date(value);
            if (Number.isNaN(date.getTime())) return '--';
            return new Intl.DateTimeFormat('pt-BR', { dateStyle: 'medium', timeStyle: 'short', timeZone: timeZone || 'UTC' }).format(date);
        }
        function formatDateShort(value) {
            const date = new Date(`${String(value || '').slice(0, 10)}T00:00:00Z`);
            if (Number.isNaN(date.getTime())) return '--';
            return new Intl.DateTimeFormat('pt-BR', { day: '2-digit', month: '2-digit', timeZone: 'UTC' }).format(date);
        }
        function formatDateLong(value) {
            const date = new Date(`${String(value || '').slice(0, 10)}T00:00:00Z`);
            if (Number.isNaN(date.getTime())) return '--';
            return new Intl.DateTimeFormat('pt-BR', { day: '2-digit', month: '2-digit', year: 'numeric', timeZone: 'UTC' }).format(date);
        }
        function formatPeriodLabel(period) { return ({ '3m': 'Ultimos 3 meses', '6m': 'Ultimos 6 meses', '12m': 'Ultimos 12 meses' })[String(period || '')] || 'Periodo padrao'; }
        function formatMetricLabel(metric) { return ({ recebido: 'Recebido', emprestado: 'Emprestado', lucro: 'Lucro' })[String(metric || '')] || 'Recebido'; }
        function buildKpiInsight(currentValue, previousValue) {
            const current = Number(currentValue) || 0;
            const previous = Number(previousValue) || 0;
            if (Math.abs(previous) < 0.00001) return Math.abs(current) < 0.00001 ? { tone: 'neutral', text: '0,00% vs mes ant.' } : { tone: current >= 0 ? 'positive' : 'negative', text: 'Sem base ant.' };
            const pct = ((current - previous) / Math.abs(previous)) * 100;
            if (Math.abs(pct) < 0.005) return { tone: 'neutral', text: '0,00% vs mes ant.' };
            const pctText = Math.abs(pct).toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
            return { tone: pct > 0 ? 'positive' : 'negative', text: `${pct > 0 ? '+' : '-'}${pctText}% vs mes ant.` };
        }
        function renderSparkline(svgElement, rawSeries, tone) {
            if (!svgElement) return;
            const values = Array.isArray(rawSeries) ? rawSeries.map((point) => typeof point === 'number' ? Number(point) : Number(point?.value)).filter((value) => Number.isFinite(value)) : [];
            if (!values.length) { svgElement.innerHTML = ''; return; }
            const color = tone === 'positive' ? '#10B981' : tone === 'negative' ? '#F87171' : '#9CA3AF';
            const fillColor = tone === 'positive' ? 'rgba(16,185,129,.15)' : tone === 'negative' ? 'rgba(248,113,113,.15)' : 'rgba(123,134,155,.13)';
            const glowColor = tone === 'positive' ? 'rgba(16,185,129,.35)' : tone === 'negative' ? 'rgba(248,113,113,.35)' : 'rgba(99,102,241,.35)';
            const minValue = Math.min(...values);
            const maxValue = Math.max(...values);
            const range = maxValue - minValue || 1;
            const points = values.map((value, index) => ({ x: values.length > 1 ? (index * 100) / (values.length - 1) : 50, y: 3 + ((1 - ((value - minValue) / range)) * 28) }));
            const linePath = points.map((point, index) => `${index === 0 ? 'M' : 'L'} ${point.x.toFixed(2)} ${point.y.toFixed(2)}`).join(' ');
            const areaPath = `${linePath} L ${points[points.length - 1].x.toFixed(2)} 33 L ${points[0].x.toFixed(2)} 33 Z`;
            svgElement.innerHTML = `<path d="${areaPath}" fill="${fillColor}"></path><path d="${linePath}" fill="none" stroke="${color}" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="filter: drop-shadow(0 0 8px ${glowColor});"></path>`;
        }
        function applyChipTone(element, tone, text) {
            if (!element) return;
            element.classList.remove('text-emerald-200', 'border-emerald-400/20', 'bg-emerald-500/10', 'text-rose-200', 'border-rose-400/20', 'bg-rose-500/10');
            if (tone === 'positive') element.classList.add('text-emerald-200', 'border-emerald-400/20', 'bg-emerald-500/10');
            if (tone === 'negative') element.classList.add('text-rose-200', 'border-rose-400/20', 'bg-rose-500/10');
            element.textContent = text;
        }
        function escapeHtml(value) {
            return String(value ?? '')
                .replaceAll('&', '&amp;')
                .replaceAll('<', '&lt;')
                .replaceAll('>', '&gt;')
                .replaceAll('"', '&quot;')
                .replaceAll("'", '&#39;');
        }
        function notify(message, type = 'success') {
            if (typeof showMessage === 'function') {
                showMessage(message, type);
                return;
            }
            window.alert(message);
        }
        function getLocalTodayDateInputValue() {
            const now = new Date();
            const year = now.getFullYear();
            const month = String(now.getMonth() + 1).padStart(2, '0');
            const day = String(now.getDate()).padStart(2, '0');
            return `${year}-${month}-${day}`;
        }
        function renderOperationList(containerId, items, emptyTitle, emptyNote, amountTone) {
            const container = document.getElementById(containerId);
            if (!container) return;
            if (!Array.isArray(items) || items.length === 0) {
                container.innerHTML = `
                    <div class="rounded-2xl border border-slate-700/60 bg-slate-950/30 px-4 py-10 text-center">
                        <div class="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl border border-slate-700/70 bg-slate-900/70 text-slate-400">
                            <i class="fas fa-calendar-check text-xl"></i>
                        </div>
                        <p class="mt-4 text-base font-semibold text-slate-100">${escapeHtml(emptyTitle)}</p>
                        <p class="mt-2 text-sm leading-6 text-slate-400">${escapeHtml(emptyNote)}</p>
                    </div>
                `;
                return;
            }

            container.innerHTML = items.map((item) => `
                <a href="${escapeHtml(item.href || '#')}" class="rounded-2xl border border-slate-700/60 bg-slate-950/30 px-4 py-4 transition hover:-translate-y-0.5 hover:border-slate-500/50 hover:bg-slate-900/70">
                    <div class="flex items-start justify-between gap-3">
                        <div class="min-w-0">
                            <div class="mb-2 flex flex-wrap gap-2">
                                <span class="inline-flex min-h-[28px] items-center rounded-full border border-slate-600/60 bg-slate-900/80 px-3 text-[11px] font-extrabold uppercase tracking-[0.12em] text-slate-200">${escapeHtml(item.typeLabel || '-')}</span>
                                <span class="inline-flex min-h-[28px] items-center rounded-full border border-slate-700/60 bg-slate-950/70 px-3 text-[11px] font-bold uppercase tracking-[0.12em] text-slate-400">${escapeHtml(item.moduleLabel || '-')}</span>
                            </div>
                            <p class="truncate text-base font-bold text-slate-50">${escapeHtml(item.title || '-')}</p>
                            <p class="mt-1 text-sm leading-6 text-slate-400">${escapeHtml(item.subtitle || '-')}</p>
                        </div>
                        <div class="shrink-0 text-right">
                            <p class="text-lg font-extrabold tracking-[-0.03em] ${amountTone}">${escapeHtml(formatCurrency(item.amount))}</p>
                        </div>
                    </div>
                </a>
            `).join('');
        }
        function renderHero(payload) {
            document.getElementById('heroGeneratedAt').textContent = formatDateTime(payload?.meta?.generatedAt, payload?.meta?.timezone);
            document.getElementById('heroPeriodChip').textContent = `Periodo: ${formatPeriodLabel(payload?.meta?.period)}`;
        }
        function renderOverviewSummary(payload) {
            const summary = payload?.overviewSummary || {};
            const cash = summary.cashBalance || {};
            const receivable = summary.accountsReceivable || {};
            const payable = summary.accountsPayable || {};
            const projected = summary.projectedBalance || {};

            const cashHref = cash.href || '/admin/dashboard.html';
            const receivableHref = receivable.href || '/admin/contas-a-receber.html';
            const payableHref = payable.href || '/admin/contas-a-pagar.html';
            const projectedHref = projected.href || '/admin/visao-geral.html';

            document.getElementById('overviewCashCard').href = cashHref;
            document.getElementById('overviewReceivableCard').href = receivableHref;
            document.getElementById('overviewPayableCard').href = payableHref;
            document.getElementById('overviewProjectedCard').href = projectedHref;

            document.getElementById('overviewCashValue').textContent = formatCurrency(cash.value || 0);
            document.getElementById('overviewCashNote').textContent = cash.note || 'Disponivel agora';
            document.getElementById('overviewCashMeta').textContent = `Ajustes: ${formatCurrency(payload?.cashAdjustment?.net || 0)}`;

            document.getElementById('overviewReceivableValue').textContent = formatCurrency(receivable.value || 0);
            document.getElementById('overviewReceivableNote').textContent = receivable.note || 'Emprestimos + Financeiro';
            document.getElementById('overviewReceivableMeta').textContent = `Emprestimos: ${formatCurrency(receivable.loanValue || 0)} | Financeiro: ${formatCurrency(receivable.financeValue || 0)}`;

            document.getElementById('overviewPayableValue').textContent = formatCurrency(payable.value || 0);
            document.getElementById('overviewPayableNote').textContent = payable.note || 'Compromissos pendentes';
            document.getElementById('overviewPayableMeta').textContent = `${Number(payable.itemsCount || 0)} lancamento(s) pendente(s)`;

            document.getElementById('overviewProjectedValue').textContent = formatCurrency(projected.value || 0);
            document.getElementById('overviewProjectedNote').textContent = projected.note || 'Apos entradas e saidas';
            document.getElementById('overviewProjectedMeta').textContent = `Entradas: ${formatCurrency(projected.receivableValue || 0)} | Saidas: ${formatCurrency(projected.payableValue || 0)}`;
        }
        function renderDailySummary(payload) {
            const operations = payload?.dailyOperations || {};
            const overview = payload?.overviewSummary || {};
            const receiptsToday = operations.receiptsToday || { totalValue: 0, items: [] };
            const paymentsToday = operations.paymentsToday || { totalValue: 0, items: [] };
            const cashBalance = Number(overview?.cashBalance?.value || 0);
            const incomingValue = Number(receiptsToday.totalValue || 0);
            const outgoingValue = Number(paymentsToday.totalValue || 0);
            const projectedDayValue = cashBalance + incomingValue - outgoingValue;
            const incomingCount = Array.isArray(receiptsToday.items) ? receiptsToday.items.length : 0;
            const outgoingCount = Array.isArray(paymentsToday.items) ? paymentsToday.items.length : 0;

            document.getElementById('summaryIncomingToday').href = overview?.accountsReceivable?.href || '/admin/contas-a-receber.html';
            document.getElementById('summaryIncomingTodayValue').textContent = formatCurrency(incomingValue);
            document.getElementById('summaryIncomingTodayMeta').textContent = incomingCount
                ? `${incomingCount} ${incomingCount === 1 ? 'entrada prevista para hoje.' : 'entradas previstas para hoje.'}`
                : 'Nenhuma entrada prevista para hoje.';

            document.getElementById('summaryOutgoingToday').href = overview?.accountsPayable?.href || '/admin/contas-a-pagar.html';
            document.getElementById('summaryOutgoingTodayValue').textContent = formatCurrency(outgoingValue);
            document.getElementById('summaryOutgoingTodayMeta').textContent = outgoingCount
                ? `${outgoingCount} ${outgoingCount === 1 ? 'saida prevista para hoje.' : 'saidas previstas para hoje.'}`
                : 'Nenhuma saida prevista para hoje.';

            document.getElementById('summaryProjectedToday').href = overview?.projectedBalance?.href || '/admin/visao-geral.html';
            document.getElementById('summaryProjectedTodayValue').textContent = formatCurrency(projectedDayValue);
            document.getElementById('summaryProjectedTodayMeta').textContent = `Caixa atual ${formatCurrency(cashBalance)} + entradas - saidas do dia.`;
        }
        function renderDailyOperations(payload) {
            const operations = payload?.dailyOperations || {};
            const receiptsToday = operations.receiptsToday || { totalValue: 0, items: [] };
            const paymentsToday = operations.paymentsToday || { totalValue: 0, items: [] };
            const alerts = operations.alerts || {};
            const projected = payload?.overviewSummary?.projectedBalance || {};
            const projectedValue = Number(projected.value || 0);
            const projectedThreshold = 1000;
            const paymentsDueTodayCount = Array.isArray(paymentsToday.items) ? paymentsToday.items.length : 0;
            const overdueOutgoingCount = Number(alerts.overdueOutgoingCount) || 0;
            const overdueIncomingCount = Number(alerts.overdueIncomingCount) || 0;
            const financialAlertsList = document.getElementById('financialAlertsList');

            document.getElementById('receiptsTodayTotal').textContent = `Total: ${formatCurrency(receiptsToday.totalValue || 0)}`;
            document.getElementById('paymentsTodayTotal').textContent = `Total: ${formatCurrency(paymentsToday.totalValue || 0)}`;

            renderOperationList(
                'receiptsTodayList',
                receiptsToday.items,
                'Sem recebimentos previstos para hoje.',
                'Quando houver parcelas ou contas a receber no dia, elas aparecem aqui.',
                'text-emerald-100',
            );
            renderOperationList(
                'paymentsTodayList',
                paymentsToday.items,
                'Sem pagamentos previstos para hoje.',
                'Quando houver contas a pagar no dia, elas aparecem aqui.',
                'text-rose-100',
            );

            if (!financialAlertsList) return;

            const alertItems = [
                {
                    href: alerts.outgoingHref || '/admin/contas-a-pagar.html',
                    icon: 'fa-bell',
                    tone: paymentsDueTodayCount ? 'amber' : 'slate',
                    label: paymentsDueTodayCount
                        ? `${paymentsDueTodayCount} ${paymentsDueTodayCount === 1 ? 'conta vence hoje' : 'contas vencem hoje'}`
                        : 'Nenhuma conta vence hoje',
                },
                {
                    href: alerts.outgoingHref || '/admin/contas-a-pagar.html',
                    icon: 'fa-circle-exclamation',
                    tone: overdueOutgoingCount ? 'rose' : 'slate',
                    label: overdueOutgoingCount
                        ? `${overdueOutgoingCount} ${overdueOutgoingCount === 1 ? 'conta atrasada' : 'contas atrasadas'}`
                        : 'Nenhuma conta atrasada',
                },
                {
                    href: alerts.incomingHref || '/admin/installments.html?status=overdue&due=month',
                    icon: 'fa-clock-rotate-left',
                    tone: overdueIncomingCount ? 'amber' : 'slate',
                    label: overdueIncomingCount
                        ? `${overdueIncomingCount} ${overdueIncomingCount === 1 ? 'recebimento atrasado' : 'recebimentos atrasados'}`
                        : 'Nenhum recebimento atrasado',
                },
                {
                    href: projected.href || '/admin/visao-geral.html',
                    icon: 'fa-wave-square',
                    tone: projectedValue < projectedThreshold ? 'violet' : 'sky',
                    label: projectedValue < projectedThreshold
                        ? `Saldo projetado < ${formatCurrency(projectedThreshold)}`
                        : `Saldo projetado: ${formatCurrency(projectedValue)}`,
                },
            ];

            const toneClasses = {
                amber: {
                    card: 'border-amber-400/15 bg-amber-500/10 hover:border-amber-300/25',
                    icon: 'bg-amber-400/10 text-amber-100',
                    text: 'text-amber-50',
                },
                rose: {
                    card: 'border-rose-400/15 bg-rose-500/10 hover:border-rose-300/25',
                    icon: 'bg-rose-400/10 text-rose-100',
                    text: 'text-rose-50',
                },
                violet: {
                    card: 'border-fuchsia-400/15 bg-fuchsia-500/10 hover:border-fuchsia-300/25',
                    icon: 'bg-fuchsia-400/10 text-fuchsia-100',
                    text: 'text-fuchsia-50',
                },
                sky: {
                    card: 'border-sky-400/15 bg-sky-500/10 hover:border-sky-300/25',
                    icon: 'bg-sky-400/10 text-sky-100',
                    text: 'text-sky-50',
                },
                slate: {
                    card: 'border-slate-700/60 bg-slate-950/30 hover:border-slate-500/50 hover:bg-slate-900/70',
                    icon: 'bg-slate-800/80 text-slate-300',
                    text: 'text-slate-100',
                },
            };

            financialAlertsList.innerHTML = alertItems.map((item) => {
                const tone = toneClasses[item.tone] || toneClasses.slate;
                return `
                    <a href="${escapeHtml(item.href || '#')}" class="group flex items-center gap-3 rounded-2xl border px-3.5 py-3 transition hover:-translate-y-0.5 ${tone.card}">
                        <span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl ${tone.icon}">
                            <i class="fas ${item.icon}"></i>
                        </span>
                        <span class="min-w-0 flex-1 text-sm font-semibold leading-5 ${tone.text}">${escapeHtml(item.label || '-')}</span>
                        <i class="fas fa-chevron-right shrink-0 text-[11px] text-slate-500 transition group-hover:text-slate-300"></i>
                    </a>
                `;
            }).join('');
        }
        function destroyOverviewMonthlyChart() {
            if (!overviewMonthlyChart) return;
            overviewMonthlyChart.destroy();
            overviewMonthlyChart = null;
        }
        function renderMonthlyFlow(payload) {
            const chartPayload = payload?.chart || {};
            const points = Array.isArray(chartPayload.points) ? chartPayload.points : [];
            const currentPoint = points.length ? points[points.length - 1] : null;
            const previousPoint = points.length >= 2 ? points[points.length - 2] : null;
            const currentValue = Number(currentPoint?.received ?? currentPoint?.value ?? 0);
            const previousValue = Number(previousPoint?.received ?? previousPoint?.value ?? 0);
            const currentOpen = Number(currentPoint?.open ?? 0);
            const currentOverdue = Number(currentPoint?.overdue ?? 0);
            const flowInsight = buildKpiInsight(currentValue, previousValue);
            const emptyState = document.getElementById('overviewChartEmpty');

            document.getElementById('flowCurrentValue').textContent = formatCurrency(currentValue);
            document.getElementById('flowCurrentNote').textContent = currentPoint
                ? `${formatMetricLabel(chartPayload.metric)} em ${currentPoint.label || 'mes atual'}.`
                : 'Sem dados no periodo atual.';
            document.getElementById('flowPreviousValue').textContent = previousPoint ? formatCurrency(previousValue) : '--';
            document.getElementById('flowPreviousNote').textContent = previousPoint
                ? `${flowInsight.text} sobre ${previousPoint.label}.`
                : 'Sem base anterior para comparativo.';
            document.getElementById('flowExposureValue').textContent = formatCurrency(currentOpen + currentOverdue);
            document.getElementById('flowExposureNote').textContent = `Em aberto: ${formatCurrency(currentOpen)} | Atrasado: ${formatCurrency(currentOverdue)}`;

            const hasData = Boolean(chartPayload.hasData) && points.length > 0;
            if (!hasData || typeof Chart === 'undefined') {
                destroyOverviewMonthlyChart();
                if (emptyState) emptyState.classList.remove('hidden');
                return;
            }

            if (emptyState) emptyState.classList.add('hidden');

            const canvas = document.getElementById('overviewMonthlyChart');
            if (!(canvas instanceof HTMLCanvasElement)) return;
            const context = canvas.getContext('2d');
            if (!context) return;

            destroyOverviewMonthlyChart();
            overviewMonthlyChart = new Chart(context, {
                type: 'bar',
                data: {
                    labels: points.map((point) => point.label || '--'),
                    datasets: [
                        {
                            type: 'line',
                            label: 'Recebido',
                            data: points.map((point) => Number(point.received ?? point.value ?? 0)),
                            borderColor: '#34d399',
                            backgroundColor: 'rgba(52, 211, 153, 0.16)',
                            fill: true,
                            tension: 0.34,
                            borderWidth: 2.4,
                            pointRadius: 0,
                            pointHoverRadius: 4,
                            pointHitRadius: 14,
                            order: 0,
                        },
                        {
                            label: 'Em aberto',
                            data: points.map((point) => Number(point.open ?? 0)),
                            backgroundColor: 'rgba(59, 130, 246, 0.32)',
                            borderColor: 'rgba(96, 165, 250, 0.7)',
                            borderWidth: 1,
                            borderRadius: 8,
                            order: 1,
                        },
                        {
                            label: 'Atrasado',
                            data: points.map((point) => Number(point.overdue ?? 0)),
                            backgroundColor: 'rgba(248, 113, 113, 0.28)',
                            borderColor: 'rgba(252, 165, 165, 0.75)',
                            borderWidth: 1,
                            borderRadius: 8,
                            order: 2,
                        },
                    ],
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        mode: 'index',
                        intersect: false,
                    },
                    plugins: {
                        legend: {
                            labels: {
                                color: '#cbd5e1',
                                boxWidth: 10,
                                boxHeight: 10,
                                usePointStyle: true,
                                pointStyle: 'circle',
                                padding: 18,
                                font: {
                                    size: 11,
                                    weight: '700',
                                },
                            },
                        },
                        tooltip: {
                            backgroundColor: 'rgba(15, 23, 42, 0.96)',
                            borderColor: 'rgba(148, 163, 184, 0.28)',
                            borderWidth: 1,
                            titleColor: '#f8fafc',
                            bodyColor: '#e2e8f0',
                            padding: 12,
                            callbacks: {
                                label: (context) => `${context.dataset.label}: ${formatCurrency(context.parsed.y || 0)}`,
                            },
                        },
                    },
                    scales: {
                        x: {
                            grid: {
                                display: false,
                            },
                            ticks: {
                                color: '#94a3b8',
                                font: {
                                    size: 11,
                                    weight: '600',
                                },
                            },
                        },
                        y: {
                            beginAtZero: true,
                            grid: {
                                color: 'rgba(71, 85, 105, 0.22)',
                                drawBorder: false,
                            },
                            ticks: {
                                color: '#94a3b8',
                                font: {
                                    size: 11,
                                },
                                callback: (value) => formatCurrency(Number(value) || 0),
                            },
                        },
                    },
                },
            });
        }
        function buildRecentMovementsPaginationSequence(currentPage, totalPages) {
            if (totalPages <= 5) return Array.from({ length: totalPages }, (_item, index) => index + 1);
            const sequence = [1];
            const start = Math.max(2, currentPage - 1);
            const end = Math.min(totalPages - 1, currentPage + 1);
            if (start > 2) sequence.push('ellipsis-left');
            for (let page = start; page <= end; page += 1) sequence.push(page);
            if (end < totalPages - 1) sequence.push('ellipsis-right');
            sequence.push(totalPages);
            return sequence;
        }
        function renderRecentMovementsPagination(meta = {}) {
            if (!recentMovementsPaginationShell || !recentMovementsPageInfo || !recentMovementsPagination) return;

            const totalItems = Number(meta.totalItems) || 0;
            const pageSize = Math.max(1, Number(meta.pageSize) || overviewState.recentMovementsPageSize || 6);
            const totalPages = Math.max(1, Number(meta.totalPages) || 1);
            const currentPage = Math.min(Math.max(Number(meta.page) || 1, 1), totalPages);
            const startItem = totalItems === 0 ? 0 : ((currentPage - 1) * pageSize) + 1;
            const endItem = totalItems === 0 ? 0 : Math.min(startItem + pageSize - 1, totalItems);

            overviewState.recentMovementsPage = currentPage;
            overviewState.recentMovementsPageSize = pageSize;
            overviewState.recentMovementsTotalPages = totalPages;

            if (totalItems <= pageSize) {
                recentMovementsPaginationShell.classList.add('hidden');
                recentMovementsPageInfo.textContent = '';
                recentMovementsPagination.innerHTML = '';
                return;
            }

            recentMovementsPaginationShell.classList.remove('hidden');
            recentMovementsPageInfo.textContent = `Mostrando ${startItem}-${endItem} de ${totalItems} movimentacoes`;

            const pages = buildRecentMovementsPaginationSequence(currentPage, totalPages);
            recentMovementsPagination.innerHTML = [
                `<button type="button" class="movement-pagination-btn" data-recent-movements-page="${currentPage - 1}" ${currentPage <= 1 ? 'disabled' : ''}>Anterior</button>`,
                ...pages.map((page) => {
                    if (typeof page !== 'number') return '<span class="movement-pagination-ellipsis">...</span>';
                    return `<button type="button" class="movement-pagination-btn ${page === currentPage ? 'movement-pagination-btn-active' : ''}" data-recent-movements-page="${page}" ${page === currentPage ? 'aria-current="page"' : ''}>${page}</button>`;
                }),
                `<button type="button" class="movement-pagination-btn" data-recent-movements-page="${currentPage + 1}" ${currentPage >= totalPages ? 'disabled' : ''}>Proxima</button>`,
            ].join('');
        }
        function setRecentMovementsLoading(loading) {
            if (recentMovementsSection) recentMovementsSection.classList.toggle('recent-movements-loading', loading);
        }
        function renderRecentMovements(payload) {
            const container = document.getElementById('recentMovementsList');
            const mobileContainer = recentMovementsMobileList;
            const items = Array.isArray(payload?.recentMovements) ? payload.recentMovements : [];
            const paginationMeta = payload?.recentMovementsPagination || {};
            if (!container || !mobileContainer) return;

            if (!items.length) {
                container.innerHTML = `
                    <tr>
                        <td colspan="6" class="px-4 py-10">
                            <div class="rounded-2xl border border-slate-700/60 bg-slate-950/30 px-4 py-10 text-center">
                                <div class="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl border border-slate-700/70 bg-slate-900/70 text-slate-400">
                                    <i class="fas fa-arrows-rotate text-xl"></i>
                                </div>
                                <p class="mt-4 text-base font-semibold text-slate-100">Sem movimentacoes recentes.</p>
                                <p class="mt-2 text-sm leading-6 text-slate-400">Quando houver recebimentos, ajustes ou lancamentos, eles aparecem aqui.</p>
                            </div>
                        </td>
                    </tr>
                `;
                mobileContainer.innerHTML = `
                    <div class="rounded-2xl border border-slate-700/60 bg-slate-950/30 px-4 py-10 text-center">
                        <div class="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl border border-slate-700/70 bg-slate-900/70 text-slate-400">
                            <i class="fas fa-arrows-rotate text-xl"></i>
                        </div>
                        <p class="mt-4 text-base font-semibold text-slate-100">Sem movimentacoes recentes.</p>
                        <p class="mt-2 text-sm leading-6 text-slate-400">Quando houver recebimentos, ajustes ou lancamentos, eles aparecem aqui.</p>
                    </div>
                `;
                renderRecentMovementsPagination(paginationMeta);
                return;
            }

            container.innerHTML = items.map((item) => {
                const direction = item.direction === 'out' ? 'out' : 'in';
                const pillClass = direction === 'out' ? 'movement-pill movement-pill-out' : 'movement-pill movement-pill-in';
                const incomingAmount = direction === 'in' ? formatCurrency(item.amount) : '-';
                const outgoingAmount = direction === 'out' ? formatCurrency(item.amount) : '-';
                const incomingClass = direction === 'in'
                    ? 'recent-movements-amount recent-movements-amount-in'
                    : 'recent-movements-amount recent-movements-amount-muted';
                const outgoingClass = direction === 'out'
                    ? 'recent-movements-amount recent-movements-amount-out'
                    : 'recent-movements-amount recent-movements-amount-muted';
                return `
                    <tr class="recent-movements-row" data-recent-movement-href="${escapeHtml(item.href || '#')}" tabindex="0" role="link">
                        <td class="recent-movements-date">${escapeHtml(formatDateLong(item.occurredAt))}</td>
                        <td><span class="${pillClass}">${escapeHtml(item.typeLabel || '-')}</span></td>
                        <td><span class="movement-pill movement-pill-neutral">${escapeHtml(item.moduleLabel || '-')}</span></td>
                        <td>
                            <p class="recent-movements-description-title">${escapeHtml(item.title || '-')}</p>
                            <p class="recent-movements-description-subtitle">${escapeHtml(item.subtitle || '-')}</p>
                        </td>
                        <td class="recent-movements-cell-right"><span class="${incomingClass}">${escapeHtml(incomingAmount)}</span></td>
                        <td class="recent-movements-cell-right"><span class="${outgoingClass}">${escapeHtml(outgoingAmount)}</span></td>
                    </tr>
                `;
            }).join('');
            mobileContainer.innerHTML = items.map((item) => {
                const direction = item.direction === 'out' ? 'out' : 'in';
                const pillClass = direction === 'out' ? 'movement-pill movement-pill-out' : 'movement-pill movement-pill-in';
                const signedAmount = `${direction === 'out' ? '-' : '+'}${formatCurrency(item.amount)}`;
                const amountClass = direction === 'out'
                    ? 'recent-movements-mobile-amount recent-movements-amount-out'
                    : 'recent-movements-mobile-amount recent-movements-amount-in';
                return `
                    <a href="${escapeHtml(item.href || '#')}" class="recent-movements-mobile-card" data-recent-movement-href="${escapeHtml(item.href || '#')}">
                        <div class="flex items-start justify-between gap-3">
                            <div class="min-w-0">
                                <div class="mb-2 flex flex-wrap gap-2">
                                    <span class="${pillClass}">${escapeHtml(item.typeLabel || '-')}</span>
                                    <span class="movement-pill movement-pill-neutral">${escapeHtml(item.moduleLabel || '-')}</span>
                                </div>
                                <p class="recent-movements-mobile-title">${escapeHtml(item.title || '-')}</p>
                                <p class="recent-movements-mobile-subtitle">${escapeHtml(item.subtitle || '-')}</p>
                            </div>
                            <div class="shrink-0 text-right">
                                <p class="${amountClass}">${escapeHtml(signedAmount)}</p>
                                <p class="recent-movements-mobile-date">${escapeHtml(formatDateLong(item.occurredAt))}</p>
                            </div>
                        </div>
                    </a>
                `;
            }).join('');
            renderRecentMovementsPagination(paginationMeta);
        }
        async function loadRecentMovementsPage(page) {
            const nextPage = Math.max(1, Number(page) || 1);
            if (overviewState.isPaginatingRecentMovements) return;
            if (nextPage === overviewState.recentMovementsPage) return;

            overviewState.isPaginatingRecentMovements = true;
            setRecentMovementsLoading(true);

            try {
                const payload = await fetchOverview(nextPage);
                hideError();
                renderRecentMovements(payload);
            } catch (error) {
                console.error(error);
                showError(error instanceof Error ? error.message : 'Falha ao carregar as movimentacoes recentes.');
            } finally {
                overviewState.isPaginatingRecentMovements = false;
                setRecentMovementsLoading(false);
            }
        }
        async function fetchOverview(recentMovementsPage = overviewState.recentMovementsPage) {
            const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone || 'America/Sao_Paulo';
            const params = new URLSearchParams({
                period: '6m',
                metric: 'recebido',
                tz: timezone,
                recentMovementsPage: String(Math.max(1, Number(recentMovementsPage) || 1)),
                recentMovementsPageSize: String(overviewState.recentMovementsPageSize),
            });
            const response = await fetch(`/api/dashboard?${params.toString()}`, { credentials: 'include' });
            const payload = await response.json().catch(() => null);
            if (!response.ok) throw new Error(payload?.message || 'Falha ao carregar a visao geral.');
            return payload;
        }
        function setLoading(loading) { overviewRoot.classList.toggle('loading', loading); }
        function showError(message) { overviewError.textContent = message; overviewError.classList.remove('hidden'); }
        function hideError() { overviewError.textContent = ''; overviewError.classList.add('hidden'); }
        async function loadOverview() {
            setLoading(true); hideError();
            try {
                const payload = await fetchOverview();
                renderHero(payload);
                renderOverviewSummary(payload);
                renderDailySummary(payload);
                renderDailyOperations(payload);
                renderMonthlyFlow(payload);
                renderRecentMovements(payload);
            } catch (error) {
                console.error(error);
                showError(error instanceof Error ? error.message : 'Falha ao carregar a visao geral.');
            } finally { setLoading(false); }
        }
        function setCashAdjustmentSubmitting(loading) {
            overviewState.cashAdjustmentSubmitting = loading;
            if (saveCashAdjustmentBtn instanceof HTMLButtonElement) {
                saveCashAdjustmentBtn.disabled = loading;
                saveCashAdjustmentBtn.innerHTML = loading
                    ? '<i class="fas fa-spinner fa-spin mr-2"></i>Salvando...'
                    : '<i class="fas fa-check mr-2"></i>Salvar ajuste';
            }
            if (closeCashAdjustModalBtn instanceof HTMLButtonElement) closeCashAdjustModalBtn.disabled = loading;
            if (cancelCashAdjustModalBtn instanceof HTMLButtonElement) cancelCashAdjustModalBtn.disabled = loading;
            if (cashAdjustmentTypeInput instanceof HTMLSelectElement) cashAdjustmentTypeInput.disabled = loading;
            if (cashAdjustmentAmountInput instanceof HTMLInputElement) cashAdjustmentAmountInput.disabled = loading;
            if (cashAdjustmentDateInput instanceof HTMLInputElement) cashAdjustmentDateInput.disabled = loading;
            if (cashAdjustmentDescriptionInput instanceof HTMLInputElement) cashAdjustmentDescriptionInput.disabled = loading;
        }
        function resetCashAdjustmentForm() {
            if (cashAdjustmentTypeInput instanceof HTMLSelectElement) cashAdjustmentTypeInput.value = 'income';
            if (cashAdjustmentAmountInput instanceof HTMLInputElement) cashAdjustmentAmountInput.value = '';
            if (cashAdjustmentDateInput instanceof HTMLInputElement) cashAdjustmentDateInput.value = getLocalTodayDateInputValue();
            if (cashAdjustmentDescriptionInput instanceof HTMLInputElement) cashAdjustmentDescriptionInput.value = '';
        }
        function openCashAdjustModal() {
            if (!(cashAdjustModal instanceof HTMLElement)) return;
            setCashAdjustmentSubmitting(false);
            resetCashAdjustmentForm();
            cashAdjustModal.classList.remove('hidden');
            cashAdjustModal.classList.add('flex');
            document.body.style.overflow = 'hidden';
            window.setTimeout(() => {
                if (cashAdjustmentTypeInput instanceof HTMLElement) cashAdjustmentTypeInput.focus();
            }, 0);
        }
        function closeCashAdjustModal(force = false) {
            if (!(cashAdjustModal instanceof HTMLElement)) return;
            if (overviewState.cashAdjustmentSubmitting && !force) return;
            cashAdjustModal.classList.add('hidden');
            cashAdjustModal.classList.remove('flex');
            document.body.style.overflow = '';
            setCashAdjustmentSubmitting(false);
        }
        async function submitCashAdjustment(event) {
            if (event instanceof Event) event.preventDefault();
            if (overviewState.cashAdjustmentSubmitting) return;

            const type = String(cashAdjustmentTypeInput?.value || 'income').toLowerCase();
            const rawAmount = String(cashAdjustmentAmountInput?.value || '').trim().replace(',', '.');
            const amount = Number(rawAmount);
            const date = String(cashAdjustmentDateInput?.value || '').trim();
            const description = String(cashAdjustmentDescriptionInput?.value || '').trim();

            if (!['income', 'expense'].includes(type)) return notify('Tipo de ajuste invalido.', 'error');
            if (!Number.isFinite(amount) || amount <= 0) return notify('Informe um valor maior que zero.', 'error');
            if (!date) return notify('Informe uma data valida.', 'error');

            setCashAdjustmentSubmitting(true);
            try {
                const response = await fetch('/api/dashboard/cash-adjustments', {
                    method: 'POST',
                    credentials: 'include',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        type,
                        amount,
                        date,
                        description: description || undefined,
                    }),
                });
                const payload = await response.json().catch(() => null);
                if (!response.ok) throw new Error(payload?.message || 'Falha ao salvar ajuste de caixa.');

                closeCashAdjustModal(true);
                notify('Ajuste de caixa salvo com sucesso.', 'success');
                await loadOverview();
            } catch (error) {
                console.error(error);
                notify(error instanceof Error ? error.message : 'Falha ao salvar ajuste de caixa.', 'error');
                setCashAdjustmentSubmitting(false);
            }
        }
        function syncSidebarLayout() {
            if (isMobileViewport()) { main.style.marginLeft = '0'; main.style.width = '100%'; main.style.flex = '1 1 100%'; return; }
            sidebar.classList.remove('mobile-open');
            if (document.body.classList.contains('sidebar-collapsed')) { main.style.marginLeft = '0'; main.style.width = '100%'; main.style.flex = '1 1 100%'; return; }
            main.style.marginLeft = '16rem'; main.style.width = 'calc(100% - 16rem)'; main.style.flex = '0 1 calc(100% - 16rem)';
        }
        function closeUserMenu() { if (userMenuDropdown) userMenuDropdown.classList.add('hidden'); }
        async function performLogout() { localStorage.removeItem('isLoggedIn'); localStorage.removeItem('currentUser'); await fetch('/auth/logout', { method: 'POST', credentials: 'include' }); window.location.href = '/login'; }
        async function handleUserMenuAction(action) { if (action === 'logout') return performLogout(); const allowedTabs = new Set(['profile', 'security', 'help']); window.location.href = `/admin/account.html?tab=${encodeURIComponent(allowedTabs.has(action) ? action : 'profile')}`; }
        sidebarToggle.addEventListener('click', () => { if (isMobileViewport()) { sidebar.classList.toggle('mobile-open'); return; } document.body.classList.toggle('sidebar-collapsed'); syncSidebarLayout(); });
        if (openCashAdjustModalBtn instanceof HTMLElement) {
            openCashAdjustModalBtn.addEventListener('click', (event) => {
                event.preventDefault();
                event.stopPropagation();
                openCashAdjustModal();
            });
            openCashAdjustModalBtn.addEventListener('keydown', (event) => {
                if (!(event instanceof KeyboardEvent)) return;
                if (event.key !== 'Enter' && event.key !== ' ') return;
                event.preventDefault();
                event.stopPropagation();
                openCashAdjustModal();
            });
        }
        if (closeCashAdjustModalBtn instanceof HTMLButtonElement) closeCashAdjustModalBtn.addEventListener('click', () => closeCashAdjustModal());
        if (cancelCashAdjustModalBtn instanceof HTMLButtonElement) cancelCashAdjustModalBtn.addEventListener('click', () => closeCashAdjustModal());
        if (cashAdjustForm instanceof HTMLFormElement) cashAdjustForm.addEventListener('submit', submitCashAdjustment);
        const recentMovementsList = document.getElementById('recentMovementsList');
        function openRecentMovementFromTarget(target) {
            if (!(target instanceof HTMLElement)) return;
            const row = target.closest('[data-recent-movement-href]');
            if (!(row instanceof HTMLElement)) return;
            const href = row.getAttribute('data-recent-movement-href');
            if (!href || href === '#') return;
            window.location.href = href;
        }
        [recentMovementsList, recentMovementsMobileList].forEach((list) => {
            if (!list) return;
            list.addEventListener('click', (event) => openRecentMovementFromTarget(event.target));
            list.addEventListener('keydown', (event) => {
                if (!(event instanceof KeyboardEvent)) return;
                if (event.key !== 'Enter' && event.key !== ' ') return;
                event.preventDefault();
                openRecentMovementFromTarget(event.target);
            });
        });
        if (recentMovementsPagination) {
            recentMovementsPagination.addEventListener('click', async (event) => {
                const target = event.target;
                if (!(target instanceof HTMLElement)) return;
                const button = target.closest('[data-recent-movements-page]');
                if (!(button instanceof HTMLButtonElement) || button.disabled) return;
                const nextPage = Number(button.getAttribute('data-recent-movements-page'));
                if (!Number.isFinite(nextPage)) return;
                await loadRecentMovementsPage(nextPage);
            });
        }
        userMenuBtn.addEventListener('click', (event) => { event.stopPropagation(); userMenuDropdown.classList.toggle('hidden'); });
        document.addEventListener('click', async (event) => {
            const target = event.target;
            if (!(target instanceof HTMLElement)) return;
            const actionBtn = target.closest('[data-user-menu-action]');
            if (actionBtn) { const action = actionBtn.getAttribute('data-user-menu-action'); closeUserMenu(); if (action) await handleUserMenuAction(action); return; }
            if (!target.closest('#userMenuBtn') && !target.closest('#userMenuDropdown')) closeUserMenu();
            if (target === cashAdjustModal) closeCashAdjustModal();
        });
        document.addEventListener('keydown', (event) => {
            if (!(event instanceof KeyboardEvent)) return;
            if (event.key !== 'Escape') return;
            closeCashAdjustModal();
        });
        window.addEventListener('resize', syncSidebarLayout);
        syncSidebarLayout();
        loadOverview();
    </script>
</body>
</html>
